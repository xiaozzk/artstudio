# 交付说明 —— 武僧 · 换武器（reskin_weapon1）

**状态**：MVP 已验证通过（新组件换回 Spine 骨架重渲，远处 0px 变化）。
**原素材未被修改**：`../images_original/`、`../monk.png`、`../monk.atlas`、`../monk.json` 全部保持原样。

## 本次改了什么

把 **皮肤 1 · weapon_1 插槽**（角色右手那枚蓝色四角星指虎，贴图 `weapon_11.png`）
按参考风格重做成**抛光铬钢机械指虎**。其余部件、其余皮肤未动。

| 文件 | 内容 |
|------|------|
| `weapon_11_new.png` | **新附件本体**：106×115 RGBA，可直接顶替 `../images_original/weapon_11.png` |
| `source_art_784x848.png` | 模型单体重画的原始出图（纯洋红底，未抠底），留档用 |
| `swap_render_compare.png` | 回验对比：左=原版，右=换上新组件（远处 0px 变化） |
| `component_compare.png` | 部件三连：原附件 \| 新组件（抠底）\| 缩放贴合到 106×115 |
| `stats.json` | 全流程量化指标（定位 / 尺寸 / 缩放比 / 回验） |

## 尺寸与装配

| 项 | 值 |
|----|-----|
| 原附件画布 | 106×115（alpha 占用 104×113 @ (1,1)-(104,113)） |
| 生成件 alpha 最大 XY | 632×699 |
| 缩放比 | 宽 ×0.1646 / 高 ×0.1617 → **等比 ×0.1617** |
| 产出 | 106×115，alpha 占位中心与原占位对齐 |

因为画布尺寸、alpha 占位中心都对齐了原附件，**UV / 挂点 / 图集区域都不用改**：
把 `weapon_11_new.png` 覆盖到 `../images_original/weapon_11.png` 再用
`tools/repair_spine/pipelines.py` 重打包图集即可（本次**没有**执行覆盖与重打包，按需再来）。

## 复现命令（本地免费部分）

```powershell
python tools/spine_part_swap.py locate --json assets/武僧/monk.json `
    --images assets/武僧/images_original --preview assets/武僧/复原预览图_1.png `
    --skin 1 --slot weapon_1 --out-dir tmp/reskin
python tools/spine_part_swap.py verify --json assets/武僧/monk.json `
    --images assets/武僧/images_original --preview assets/武僧/复原预览图_1.png --skin 1 `
    --part weapon_11=<本目录>/weapon_11_new.png --out-dir tmp/reskin
```

花钱那一步（部件单体重画，1 次调用 6871 tokens / 113s）与整条流水线说明见
`tools/spine-reskin.md`。**成本实测（已对过账单）**：这一轮 ZenMux 共**计费 7 次请求 = $1.2009**，
但只有 2 次真正拿到了图 —— 其余 5 次是"客户端被掐断、服务端照跑"的请求，**照样扣钱**（白烧 $0.87 / 72%）：
`background=transparent` 那 4 次各 $0.1797，另 1 次 latency 364s 的 $0.1512。
**一次图片编辑约 $0.15~0.18**（`image_output` 占 94%），别按订阅页的 flow 单价估。
核账命令：`python tools/zenmux_edit.py cost --models openai/gpt-image-2`。

## 下一步可选

* **整体换皮**：同一流程批处理其余 16 个可见部件（每件 1 次调用），预期画风统一；
* **风格融合**：若某件要保留原有扁平赛璐璐感，在 prompt 里显式写"keep flat cel shading"，
  否则默认按风格参考重绘（用户 2026-09-21 口径）；
* **入库**：替换 `images_original/weapon_11.png` + 重打包图集 + 重新导出预览复核。
