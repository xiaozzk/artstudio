# 交付说明 —— fulei_stage2

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 86 / 50 / ['default', 'black', 'white'] |
| 附件总数 | 126（图集区域 94） |
| 动画 | 12 个：attack, cast, cast2, cast3, cast4, cast5, cast6, cast7 |

## 交付物

* `fulei_stage2.atlas`  (4514 字节)
* `fulei_stage2.json`  (266177 字节)
* `fulei_stage2.png`  (1696981 字节)
* `images_original/`  (94 张单图)
* `原始资源_fulei_stage2.zip`  (1865860 字节)
* `复原预览图_black.png`  (507962 字节)
* `复原预览图_white.png`  (586065 字节)

## 处理动作

* ⚠ 有 2/60 个网格**不可信**（按网格裁会削本体）：guang_jianzhang_l(网格多半落在空白上（40% 命中内容）)、jian_light(网格多半落在空白上（31% 命中内容）)
* ⚠ 网格附件的声明尺寸疑为**占位值**（73 个网格中 12 个与图集不符、且对不上包内倍率，如 an_pijian1）—— 这些不做尺寸对齐；若按声明值缩会把网格图缩成占位尺寸（实测 32x32），再被网格拉伸成大块矩形残影
* **源图集尺寸自检**：12/126 个附件的声明尺寸与图集 `orig` 不符，其中 12 个是网格附件**占位值**（共 73 个网格）—— 真实形状由 `vertices` 决定，**未按它缩放这些单图**

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 94 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 94/94 一致（RGB 最大差 0，alpha 最大差 0） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_fulei_stage2.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_fulei_stage2.zip -o <空目录>
```

## 备注

* 含斜杠的插槽名被 Spine 重命名 14 处（4.x 不允许插槽名带 `/`）
* Spine 重置无效边 146 处（源数据的网格边索引不完整，Spine 自动修正；该素材家族的常见告警）
