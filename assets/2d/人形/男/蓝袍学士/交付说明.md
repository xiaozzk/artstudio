# 交付说明 —— 112058

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 229 / 80 / ['default'] |
| 附件总数 | 80（图集区域 64） |
| 动画 | 4 个：happy, idle, sad, state1 |

## 交付物

* `112058.atlas`  (2864 字节)
* `112058.json`  (197693 字节)
* `112058.png`  (740033 字节)
* `images_original/`  (64 张单图)
* `交付说明.md`  (1228 字节)
* `原始资源_112058.zip`  (802271 字节)
* `复原预览图.png`  (1327869 字节)

## 处理动作

* 页名 112058.webp -> 112058.png
* ⚠ 有 5/64 个网格**不可信**（按网格裁会削本体）：highlightL(网格多半落在空白上（48% 命中内容）)、muyu dec(网格多半落在空白上（49% 命中内容）)、shuangyanpiL(网格多半落在空白上（19% 命中内容）)、shuangyanpiR(网格多半落在空白上（9% 命中内容）)

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 64 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 64/64 一致（RGB 最大差 0，alpha 最大差 0） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_112058.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_112058.zip -o <空目录>
```

## 备注

* Spine 重置无效边 130 处（源数据的网格边索引不完整，Spine 自动修正；该素材家族的常见告警）
