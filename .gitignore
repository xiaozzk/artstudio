# --- OS -------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# --- Editors / IDEs --------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# --- Python (assets/_archive/scripts/ holds legacy helpers) -----------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.env.local
.env.*.local
.env/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node (in case future tooling) -----------------------------------------
node_modules/
.npm/
.yarn/
.pnp.*

# --- Logs / temp -----------------------------------------------------------
*.log
*.tmp
*.bak
*.pid
*.seed
*.pid.lock
.cache/

# --- Local artifacts -------------------------------------------------------
test-output/
out/
tmp/

# --- meowa/ 生图工作区（静态素材走 Git LFS 托管，见 .gitattributes）---------
# 只排除可再生的批量产物；其余素材 / 参考图 / 存档全部入库（由 LFS 存大文件）
meowa/runs/
meowa/qa/

# --- Nested reference repos (kept locally only) -----------------------------
reference/spine-runtimes-4.3/
# 本地安全备份：bundle 内嵌整段历史，绝不能入库
reference/_backup/

# --- Blender 自动备份（.blend1/.blend2… 是上一次的保存副本，不入库）-----------
*.blend[0-9]*