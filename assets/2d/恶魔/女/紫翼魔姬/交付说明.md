# 交付说明 —— Courier

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 102 / 116 / ['default', 'blue', 'red'] |
| 附件总数 | 123（图集区域 76） |
| 动画 | 12 个：attack1, chuchang, die, lichang, run, shouji, skill0, skill1 |

## 交付物

* `Courier.atlas`  (3323 字节)
* `Courier.json`  (370238 字节)
* `Courier.png`  (371992 字节)
* `images_original/`  (76 张单图)
* `原始资源_Courier.zip`  (375859 字节)
* `复原预览图_blue.png`  (201529 字节)
* `复原预览图_default.png`  (187697 字节)
* `复原预览图_red.png`  (198917 字节)

## 处理动作

* 页名 Courier.webp -> Courier.png
* 页图缩回声明尺寸 [512, 512] -> [588, 588]
* ⚠ 有 3/17 个网格**不可信**（按网格裁会削本体）：chibangyou(网格多半落在空白上（46% 命中内容）)、chibangzuo(网格多半落在空白上（41% 命中内容）)、weiba(网格多半落在空白上（38% 命中内容）)
* ⚠ 网格附件的声明尺寸疑为**占位值**（17 个网格中 10 个与图集不符，如 blue/qunzihou）—— 已跳过它们不做尺寸对齐；若不跳过会把网格图缩成声明值，再被网格拉伸成大块矩形残影
* 单图按声明尺寸放大 ×2.2857：0 张（源图集只有声明尺寸的 0.438 倍）
* **打包倍率还原**：源图集按声明尺寸的 0.438 倍打包（10/123 个附件不符，一次等比缩放可解释 0/10），已逐图放大 ×2.2857 还原到 JSON 声明的尺寸

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 76 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 76/76 一致（RGB 最大差 0，alpha 最大差 0） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_Courier.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_Courier.zip -o <空目录>
```
