# 交付说明 —— 30501_sanjiaotou

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 61 / 38 / ['default'] |
| 附件总数 | 40（图集区域 25） |
| 动画 | 8 个：Atk, Chuchang, Death, Run, Skill_1, Skill_2, Stand, Weak |

## 交付物

* `30501_sanjiaotou.atlas`  (984 字节)
* `30501_sanjiaotou.json`  (456447 字节)
* `30501_sanjiaotou.png`  (1123796 字节)
* `images_original/`  (25 张单图)
* `原始资源_30501_sanjiaotou.zip`  (1034699 字节)
* `复原预览图.png`  (1277823 字节)

## 处理动作

* ⚠ 页 `size:` 行过期（声明 [1462, 1462] 实际 [1463, 1463] 只差几像素、覆盖率 99.93 无法区分）—— 已按**实际图**改 `size:` 行、**不重采样**（改声明无损，缩图会让全图重采样）
* ⚠ 有 6/24 个网格**不可信**（按网格裁会削本体）：qianshouzhang(网格偏小（只解释 32% 的本体）)、qiang(网格偏小（只解释 42% 的本体）)、jian(网格偏小（只解释 52% 的本体）)、sanjiaiotou(网格偏小（只解释 58% 的本体）)
* 单图按声明尺寸放大 ×1.0076：1 张（源图集 = 声明尺寸 × 0.992）
* **源图集尺寸自检**：源图集 = 声明尺寸 × 0.992（6/40 个附件不符，一次等比缩放可解释 0/6）

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 25 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 25/25 一致（RGB 最大差 0，alpha 最大差 0） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_30501_sanjiaotou.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_30501_sanjiaotou.zip -o <空目录>
```

## 备注

* Spine 重置无效边 68 处（源数据的网格边索引不完整，Spine 自动修正；该素材家族的常见告警）
