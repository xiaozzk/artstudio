# Spine 图集资源修复通用经验

> 面向 `xxx.json` + `xxx.atlas` + `xxx.png` 三件套（Spine 4.x，常见于游戏拆包 / 美术外包交付）。
> 症状通常是「导入不显示 / 显示不全 / 部件尺寸对不上 / 出现幽灵图元」。
> 工具：`tools/repair_spine/`（纯本地确定性，不消耗额度）。两个实战场次：`狂战士`、`cha_1114`。

---

## 一、症状 → 病因 → 处理 速查表

| # | 症状 | 真正的病因 | 处理 | 出处 |
|---|---|---|---|---|
| 1 | 导入后**一张图都不显示** | `.atlas` 首行页名与磁盘页图文件名不一致（写 `xxx.webp`，磁盘是 `xxx.png`） | 改 atlas 首行 | 狂战士 |
| 2 | 编辑器里全是橙色 `MISSING` | 编辑器**不读 `.atlas`**，按附件 `path` 去图片文件夹找 `<path>.png` | 解包图集成单图 | 狂战士 |
| 3 | 感觉「还丢图」 | 插槽没有默认附件、动画附件时间轴为空 | 手工补引用 | 狂战士 |
| 4 | 部件大小对不上 / 发虚 | 图集按 **0.5 倍**打包，JSON 声明的是原图尺寸 | 把图还原到声明尺寸 | 狂战士 |
| 5 | **运行期多画幽灵图元 / 单图里混进邻居的图** | **atlas 各区域矩形互相重叠** | 去污染 + 重打包 | cha_1114 |
| 6 | 编辑器里出现**白色方块/圆块占位** | 图片是**调色板 PNG（mode `P`）+ 逐索引 `tRNS`**，解析失败 | 统一转 RGBA | cha_1114 |
| 7 | 「JSON 是不是坏了？」——导入结果与原画不符 | **多数时候 JSON 没问题**，问题在图那一侧 | 用独立渲染器证伪，见 §二 | cha_1114 |
| 8 | **自己写的预览里某些部件"没显示/位置不对"** | 合成用了**替换**语义，附件在自身透明像素处擦掉了下层 | 逐个附件做 alpha 合成，见 §3.7 | cha_1114 |

---

## 二、铁律：先分清「骨架的问题」还是「图的问题」

**这是两次实战里最值钱的一条。** 用户看到导入结果不对，第一反应往往是「JSON 坏了」；
但在 cha_1114 上，JSON 被证明**完全自洽**，坏的只是图集打包。

做法：**写一个独立渲染器**（只吃 JSON + 单图，完全不碰 atlas），用它当裁判。

```
渲染世界包围盒  ==?  JSON 里 skeleton.width/height
渲染包围盒左下角 ==?  JSON 里 skeleton.x/y
```

* **两个都对上**（cha_1114：281.17 × 445.00 vs 声明 281.15 × 445.00，左下角 -318.77/-62.42
  加回 `skeleton.x/y` 后正好等于 -159.39/-31.21）⇒ **骨架几何没问题，别再去动 JSON**。
  那么「导入显示不对」就只能出在喂进去的**图**这一侧：图集重叠、单图被污染、图片格式、图片路径指错。
* **对不上** ⇒ 才是 JSON / 骨骼 / 附件数据的问题。

配套结论：
* 验证「该用哪一套图」用 **`meshfit`**：把网格三角形按 `UV ↔ 顶点` 的仿射投影回图像空间，
  与实际画面求 IoU。cha_1114 上：**干净单图 0.724 / 原始污染单图 0.571**，一眼分高下。
* 同一招里的 `sqrt|det|`（最小二乘仿射的行列式开方）应 **≈ 骨骼的 scale**。
  cha_1114 实测 0.956 ≈ 骨骼 `all` 的 0.95 ⇒ 图与声明尺寸是 **1:1，没有降采样**；
  狂战士实测 ≈ 2.0 ⇒ 图集是 **0.5 倍打包**，要放大还原。**同一个指标，两种结论。**

---

## 三、atlas 格式要点（都是实测踩出来的）

### 3.1 两套键名都要认

| 新式（4.1+） | 旧式（≤4.0） | 含义 |
|---|---|---|
| `bounds: x, y, w, h` | `xy: x, y` + `size: w, h` | 页内位置；w/h 是**旋转前**尺寸 |
| `offsets: ox, oy, ow, oh` | `offset: ox, oy` + `orig: ow, oh` | 从左边缘、**下边缘**裁掉的空白；裁白前原始尺寸 |
| `rotate: true/false/90/180/270` | 同 | 逆时针角度，`true ≡ 90` |

### 3.2 页内矩形：旋转要互换，**y 是自上而下**

```
页内矩形 = (deg==90 || deg==270) ? (x, y, h, w) : (x, y, w, h)
```

⚠️ **修正一处流传很广的说法**：Spine 的 atlas **y 是从页图「上边缘」向下量**的，
不是从下边缘（OpenGL 习惯）。这不是抄文档，是把 8 种约定（是否互换 × 4 种 y 轴向）穷举出来、
用「越界数 / 与真实连通域 bbox 的 IoU / 是否构成页图不透明像素的精确分区」三项指标排出来的：

| 约定 | 越界 | 与连通域平均 IoU | 分区性 |
|---|---|---|---|
| **互换 + y 自上而下** | **0** | **0.915** | 最好 |
| 互换 + y 自下而上 | 0 | 0.266 | 差 |
| 不互换 + y 自上而下 | 4 | 0.745 | 差 |

**教训：约定不要靠记忆和文档，要靠穷举 + 客观指标。**

### 3.3 空行 = 换页（写 atlas 时最容易翻车的地方）

官方解析器逻辑：`空行 → page = null`，下一个非空行即新页。所以

* 区域之间**不要**空行；
* **页属性之后也不能有空行** —— 否则第一个区域名会被当成新页名，整个文件解析错位。
  （这个坑是自己生成 atlas 时踩到的：少写一个空行就对了。）

### 3.4 裁白偏移的量法

```
offsetRight = ow - w - ox
offsetTop   = oh - oy - h          # oy 从下边缘量
贴回原画布锚点 = (ox, oh - oy - packedH)   # 左下角
```

给编辑器喂图时必须还原到**裁白前的原始画布**，否则 UV / 顶点会错位。

### 3.5 region 附件的 `(x, y)` 是**区域中心**

不是左下角。验证方式：拿编辑器截图当基准，穷举 center / bottom-left / bottom-right /
top-left / top-right 五种锚点比轮廓 IoU，center 明显最优（0.434 vs 0.415/0.350/0.304/0.353）。

### 3.6 网格顶点的「加权格式」

* `len(vertices) == len(uvs)` → 普通网格，直接 (x, y) 成对；
* 长度不等 → 加权网格，格式为 `骨数, [骨骼索引, x, y, 权重] × 骨数, ...`；
* **骨骼索引是全局骨骼序号**，不需要也从来不用 `bones` 名字数组（那是 skin 的字段，容易被误导）。
* 网格 `regionUVs` 归一化在**未裁白原尺寸**上，(0,0) = 左上。

自检：解析后 「消耗的数组长度 == len(vertices)」且「产出顶点数 == len(uvs)/2」且
「最大骨索引 < 骨骼总数」，三项都过才算解析正确。

### 3.7 合成必须按附件做 alpha 合成（over）

自己写预览渲染器时最阴的一个坑：

```python
# ✗ 错的：paste + 三角形 mask = "替换"语义
canvas.paste(tex, (0, 0), tri_mask)
```

`paste(im, box, mask)` 的语义是 `dst = dst*(1-m) + src*m`。当 `m=255` 时 `dst = src`，
**src 的 alpha 被直接写进去** —— 于是附件在自己**透明**的像素处会把下层已经画好的部件**擦掉**。

症状极具欺骗性：看起来像「某些部件没被读取 / 位置没显示」。cha_1114 就是这样：
斗笠 `hair_1`（slot 最后、231×90 的四边形）一盖，下面的 `head_2`（脸）和 `hair_10`/`hair_12`
整块被擦没 —— 而它们的**几何位置其实全对**（包围盒与声明一致），
逐件单独渲染也都在正确位置，所以会一直往「读没读进来」的方向查错。

```python
# ✓ 对的：一个附件的所有三角形先画进独立的一层，再整层 alpha 合成
layer = Image.new('RGBA', canvas.size, (0, 0, 0, 0))
for (i, j, k) in tri:
    layer.paste(src.transform(...), (0, 0), tri_mask)   # 同附件内三角形互不重叠，安全
canvas.alpha_composite(layer)                            # 只有真正重叠的不透明部分才遮挡
```

**判据：只有几何上真正重叠的不透明像素，才应该产生遮挡。**
验证手法：逐件「抽掉它再渲染，看画面变不变」，统计有多少件 100% 不可见 ——
按正确合成，可见件数应明显回升。

---

## 四、排查方法论（下次照这个顺序做）

1. **页名**：`.atlas` 首行 ↔ 磁盘页图文件名，逐字符一致（含扩展名）。
2. **三方对账**：JSON 引用（注意 `path` 优先于附件名）/ atlas 区域名 / 磁盘文件名。
3. **分区性检验（最硬的指标）**：
   ```
   各区域矩形内不透明像素之和  ==?  页图不透明像素总数
   ```
   超过 ⇒ 矩形互相重叠。cha_1114：234,005 vs 172,526，**多出 61,479**，
   58 对矩形重叠、58,887 个像素被 ≥2 个矩形覆盖 ⇒ 判定图集非法。
   附带：`被 ≥2 个矩形覆盖的不透明像素 == 0` 且 `未被覆盖 == 0` 才算合法图集。
4. **尺寸倍率**：`声明尺寸 ÷ 图片尺寸`，恒定倍数 ⇒ 降采样打包。
5. **图片格式**：有非 RGBA 的（尤其 mode `P` + `tRNS`）先转 RGBA。
6. **别信直觉，做对照实验**：改一个变量重渲染，用包围盒 / IoU 说话。
7. **写独立渲染器**当裁判，把「骨架问题」和「图问题」彻底分开（§二）。

---

## 五、修复流程

### 5.0 节奏：一条命令走完，**阶段之间不需要人工确认**（**硬性**）

主流程压成**一条命令**，`prepare -> convert -> apply` 之间**不停下等确认**：

```bash
python tools/repair_spine/pipelines.py build <原始.zip> -o <工程目录>
# 解压 → 页名/页尺寸修复 → 打包倍率还原 → 解包去污染 → 转目标版本
# → 交付顶层三件套 → 复原预览图.png + 交付说明.md
```

**为什么不需要确认**：以前靠"看一眼预览再决定下一步"，现在这一步被
**自动渲染比对**替代了 —— `apply` 会逐皮肤比 `prepare/` 与 `convert/` 的渲染，
一致才替换顶层，不一致就两份都留并出对比图交人工。人只在**最后**看交付件与预览。

原始三件套始终归档在
`原始资源_<项目>.zip`，随时可回退（`build 原始资源_*.zip -o <空目录>` 即可重建）。

设计要点，都是踩过才定的：

* **倍率修复放在「解包」这一步**，而不是转换前 —— 它属于解包的完成度（图出来了但尺寸不是
  JSON 声明的尺寸，这活没干完）；预览图看不出差别（渲染按 JSON 声明尺寸画四边形，
  纹理放大与否几何相同）；而且 `convert` 刻意只吃「JSON + 单图」、不碰 atlas，
  塞进去会破坏分层。
* **`prepare`/`convert`/`apply` 三段仍保留**，用于分步排查；`prepare`/`convert` 只写新增文件、
  绝不覆盖已交付的顶层三件套，替换只发生在 `apply`。
* `apply` 会**自动生成 `交付说明.md`**：版本、处理动作、校验数字、文件清单、回退方法都现场采集，
  连"源图集打包倍率是多少"都从归档里重新算一遍 —— 交付件自带完整履历。
* 中间态输出要**紧凑**。早先每步都往外倒一整坨 JSON，没人看得下去；
  现在每步只有几行结论（`--check` 才补细节）。

### 5.1 分步命令（排查用）

主流程是一条命令，但同一套流水线的三段都能单独跑 —— 卡在某一步时用它们定位：

| 阶段 | 命令 | 产出 | 说明 |
|---|---|---|---|
| **1 解包** | `prepare <zip\|目录> -o <项目目录>` | 顶层三件套 + `images_original/`（去污染 + 倍率还原）+ `复原预览图.png` + `原始资源_<项目>.zip` | `--check` 才打三方对账 |
| **2 转换** | `convert <项目目录>` | `<项目>/spine4.3.26/`（json + atlas + png + 预览），**不动顶层** | |
| **3 交付** | `apply <项目目录>` | 顶层被目标版本覆盖，删掉 `spineXXXX/`，生成 `交付说明.md` | 原始版本仍在 `原始资源_*.zip` |

**纪律**：阶段 1/2 **只新增、绝不覆盖**已交付的顶层三件套；替换只发生在 `apply`（或一条龙的
`build`）。这样"转换没做好"永远不会污染已经确认过的产物。

**版本转换必须走 Spine 官方链路，不要手写格式转换器。**
4.x 拒绝直读 3.8 的导出数据（`The Spine version must be the same version that exported the data file`），
必须：**匹配版本导入成 `.spine` 工程 → 目标版本打开工程（自动升级）导出**：

```bat
Spine.com -u 3.8.99  -i cha_1114.json -o cha_1114.spine -r          :: A 匹配版本导入
Spine.com -u 4.3.26  -i cha_1114.spine -o <输出目录> -e json+pack    :: B 目标版本导出
```

版本包在 `%USERPROFILE%\Spine\updates\` 下（本地已下载的版本号直接可用，不必联网）。

**3.8 → 4.3 实际会变的字段**（Spine 自动完成，但你要知道会变）：

| 变化 | 内容 |
|---|---|
| `skeleton` | `x / y / width / height` **被移除**（4.x 起是编辑器专用，不进 SkeletonData） |
| 骨骼 | `transform` → **`inherit`** |
| **插槽名** | 4.x **禁止 `/`**：`cha_4103/hair_12` → `cha_4103-hair_12`（**插槽名会变，代码里按名引用的地方要同步**） |
| 附件名 | 不变 |
| 约束 | `ik: [...]` → **`constraints: [{type:"ik", ...}]`** |
| 变形时间轴 | `animation.deform` → **并入 `animation.attachments`** 的 `deform` 子键 |
| 图集 | 旧式 `xy/size/orig/offset/index` → `bounds/offsets`；**`pma:true`**（预乘 alpha）；Spine 会**重打包** |

两个副作用要知道：
1. **坐标原点位移**：`skeleton.x/y` 丢掉后，角色的世界坐标会平移（本例 +159.38, +31.21）。
   依赖 3.8 那个初始位置的工程，需要 `setPosition` 或把偏移烘进 root 骨骼。
2. **图集被重新裁白**：Spine 打包会按 alpha 阈值削掉最外圈极淡的边（alpha 1~3），
   反解回环比对时会有几个像素差 —— 用 `verify --alpha-tol` 之类的容差区分"裁剪舍入"和"真错"。

**顺带好处**：Spine 重打包出来的图集**矩形重叠为 0**，比原始 3.8 图集更规范。

### Step 1 — 修页名
`.atlas` 首行必须等于磁盘页图文件名。

### Step 2 — 去污染，导出干净单图
图集矩形重叠时，**每个矩形裁出来的图都会把落在同一矩形里的邻居一起带走**。
cha_1114：56 张单图里 24 张被污染（最多一张 68% 是别人的像素，合计 61,509 px）。

原理：打包工具是把邻居塞进本区域的**透明空白处**，所以
**一个连通域只属于一个区域**。做法：

1. 页图做 8 邻接连通域标记；
2. 每个连通域按 **像素级 IoU 最大**归属到某个区域
   （⚠️ 不要用 bbox 交集占比：`shadow` 的椭圆整块落在 `hair_8` 的矩形里，
   按 bbox 会误判给 `hair_8`，按 IoU 才对）；
3. 每个区域只保留属于自己那些连通域的像素，其余 alpha 置 0；
4. 再按旋转 / 裁白偏移还原成未裁白原尺寸。

外来像素与本体的连通域**天然不相交**，所以这一步**不会误删本体**。
但仍要用 `meshfit` / 渲染前后对比复核。

### Step 3 — 重打包一份合法图集
`offsets` / 裁白后尺寸 / 区域名全部不变，只重算 `bounds` 与页图；旋转一律去掉（省事且等价）；
加 padding 并把边缘 RGB 向外扩散（bleed）防 Linear 暗边。
**骨架 JSON 一个字节都不用改** —— 这是重打包相对「改 JSON」的最大优势。

### Step 4 — 指路径重新导入
工程先另存（否则相对路径没基准），把编辑器的「图片」节点路径指到单图目录。
`json` 里的 `skeleton.images` 是**编辑器用**的提示字段，指对了省事，运行期不读它。

### Step 5 — 图片格式统一
调色板 PNG（mode `P`）转 RGBA。cha_1114 的 `shadow.png` 就是唯一的非 RGBA 文件，
且带 78 项逐索引 `tRNS`；这类文件在部分编辑器里会变成**白色占位块**。

### Step 6 — 补回丢失的引用
`slots[].attachment` 为空的插槽、动画里 `attachments` 时间轴为空的动画。
⚠️ 先判断是不是**故意的备用件库**（cha_1114 有 13 个插槽没默认附件，
但都是武器/手/脚的多套变体，靠动画的附件时间轴切换，**不是缺陷**）。

### Step 7 — 交付归档（**硬性规则，别忘**）

修复产物**不允许平铺在 `assets/` 根下**，必须按「**类目 / 性别 / 项目**」归档：

```
assets/人形/男/<项目名>/
├── <项目名>.json 或 <项目名>_fullsize.json   骨架（编辑器导入用）
├── images_original/                          干净单图（编辑器图片路径指这里）
├── 复原预览图.png                             setup pose 验收基准图
├── 原始资源_<原名>.zip                        原始三件套归档（留档对照）
└── 说明/经验文档.md
```

* 已有先例：`assets/人形/男/狂战士/`、`assets/人形/男/cha_1114/`。
* `assets/` 根下只放**未处理的原始包**（如 `assets/xxx.zip`）与 `_archive/`、`eva_bone/` 这类既有目录。
* 需要直接跑运行期时，可在项目目录顶层额外保留 `xxx.atlas` + `xxx.png`
  （归档 zip 里也有同一份）—— 这是允许的便利，不是替代归档。

> 教训：cha_1114 第一次交付时我按用户口头给的 `D:\artstudio\assets\` 平铺成了
> `assets/cha_1114/`，漏了归档层级。**目的地只给到 `assets/` 时，默认按上面的规则再建一层。**

---

## 六、验收指标（都要能复算，别靠肉眼）

| 指标 | 期望 | 备注 |
|---|---|---|
| 页名 ↔ 文件名 | 一致 | |
| 三方对账 | 全中 | 名字层面 |
| 声明尺寸 vs 图片尺寸 | 全等 | |
| 网格 `width/height` vs atlas `ow,oh` | 全等 | |
| **分区性** | 重叠像素 0 / 未覆盖 0 | 判断图集合法性的硬指标 |
| **反解回环** | atlas 反解出的图 == 交付单图（alpha 逐字节 + 可见 RGB 逐字节） | 重打包后必跑 |
| **渲染包围盒** vs `skeleton` 声明 | 相等 | 骨架自洽性 |
| **网格 IoU**（网格投影 vs 画面） | 越高越好，且**换图前后要能分出高下** | 判断该用哪套图 |
| 仿射 `sqrt\|det\|` | ≈ 骨骼 scale | 判断是否降采样 |

---

## 七、工具

**唯一工具：`tools/repair_spine/pipelines.py`**（单文件，由原 5 个模块合并而成）。
子命令：`build` 一条命令跑完前三阶段；`prepare` / `convert` / `apply` 分步；
`file` 归档到 `assets/2d/`；`diagnose` / `pagefix` / `unpack` / `repack` /
`render` / `verify` / `meshfit` 为排查用。文档见 `tools/repair_spine/README.md`。

`tools/repair_spine/`（详见其 `README.md`）：

```bash
python tools/repair_spine/pipelines.py build    <原始.zip> -o <工程目录>   # 一条命令跑完 1-3
python tools/repair_spine/pipelines.py prepare  <原始.zip> -o <工程目录>   # 阶段1（排查用）
python tools/repair_spine/pipelines.py convert  <工程目录>                 # 阶段2（排查用）
python tools/repair_spine/pipelines.py apply    <工程目录>                 # 阶段3（排查用）
python tools/repair_spine/pipelines.py file     <工程目录> --category "人形/女" --as "火把女战士"
python tools/repair_spine/pipelines.py diagnose <目录>                     # 体检（含分区性检验）
python tools/repair_spine/pipelines.py unpack   <目录> -o images_original  # 去污染导出单图
python tools/repair_spine/pipelines.py repack   <目录> --from images_original -o repacked
python tools/repair_spine/pipelines.py render   <目录> --images images_original -o preview.png
python tools/repair_spine/pipelines.py verify   <目录> --images images_original
python pipelines.py meshfit  <目录> --images images_original
```

`repack` 会自动跑反解回环 + 新页重叠复检；`render` 会打印渲染包围盒与声明的对比。

---

## 八、三个案例的对照

| | 狂战士 `Unit_Spine_NowsiWarriorSpe1` | cha_1114 |
|---|---|---|
| 源版本 | Spine **4.2.11** | Spine **3.8.95** |
| 主因 | 页名写 `.webp`；编辑器不读 atlas；图集 **0.5 倍**打包 | **atlas 矩形重叠**；单图被邻居污染；`shadow.png` 是调色板 PNG |
| 分区性 | 合法 | **不合法**：多覆盖 58,887 px，58 对重叠 |
| `sqrt\|det\|` | ≈ **2.0** ⇒ 要放大还原 | ≈ **0.95** = 骨骼 scale ⇒ **1:1，不要放大** |
| JSON | 网格 `width/height` 被写死成图集尺寸，要改（→ `_fullsize.json`） | **完全自洽，一个字节没动** |
| 插槽名带 `/` | 无 | 有 → 升 4.x 时 62 个全部改名 |
| 首次交付 | `_fullsize.json` + 23 张放大单图 | 重打包 atlas + 56 张干净单图 |
| **升级到 4.3.26** | 4.2.11 →（经 4.2.43）→ 4.3.26；图集 2030×515，重叠 0；渲染 alpha 差 **4 px** | 3.8.95 →（经 3.8.99）→ 4.3.26；图集 1821×269，重叠 0；渲染 alpha 差 **9 px** |

**同一套流程，两个案例的处理动作完全不同 —— 所以每一步都必须先量、再决定。**

### 8.1 升级到 4.3.26 时踩到的三件事（都是狂战士暴露的）

1. **工程名 = 导出名。** Spine CLI 的导入/导出以**工程文件名**决定骨架名与导出文件名，
   所以工程必须命名成 `<骨架名>.spine`。狂战士的源 JSON 叫
   `Unit_Spine_NowsiWarriorSpe1_fullsize.json`，直接拿它当工程名会导出成
   `..._fullsize.json` —— 工具要自动剥掉 `_fullsize` 后缀（`convert --name`）。
2. **别依赖源 JSON 里的 `images` 字段。** 狂战士的 `_fullsize.json` 根本没有这个键。
   工具改成在临时目录写一份工作副本、把 `skeleton.images` 指到单图目录的**绝对路径**，
   既不改用户原文件，也不怕字段缺失。
3. **`apply` 要容忍"顶层没有 atlas/png"。** 有些项目目录只在归档 zip 里有三件套
   （狂战士就是这样），替换时是**新增**而不是覆盖，不能因此报错。
4. **重打包后的回环校验要用容差。** Spine 按 alpha 阈值重新裁白，会削掉最外圈
   alpha 1~3 的边（cha_1114：`l_hand_d` 4px / `shadow` 320px，最大差 3；
   狂战士：`Rhand2`，最大差 3）。这是"裁剪舍入"不是"真错" ——
   工具用 `alpha_tol`（默认 4）区分；同时 RGB 改到**预乘域**比较，容差 1，比反预乘紧得多。

### 8.2 批量六包（同一批下载，问题各不相同）

2026-09 一批处理了 6 个包，**全部是 3.8.95**，走同一条 3.8.99 → 4.3.26 官方链路。
但每包踩的坑都不一样 —— 再次印证「每一步先量再决定」：

| 项目 | 骨骼/插槽/区域 | 图集问题 | 处理 |
|---|---|---|---|
| `qiaofeng` | 52 / 16 / 16 | **无**（矩形精确分区） | 直接过 |
| `NPC2` | 24 / 19 / 19 | 仅页名写 `.webp` | 页名规范化 |
| `Kimchul` | 171 / 143 / 182 | 页名 `.webp`；**零间距打包**（重叠对 0，但相邻精灵粘连） | 页名规范化；**去污染必须是空操作** |
| `cha_4205` | 110 / 66 / 63 | 54 对重叠 / 20,431 px 多覆盖；含**别名区域** | 去污染 + 别名分组 |
| `cha_1166` | 140 / 79 / 69 | 99 对重叠 / 65,597 px 多覆盖；含别名区域 | 去污染 + 别名分组 |
| `Nomad` | 38 / 27 / 43 | 页名 `.webp`；**页图被非等比重采样**；78 对重叠 / 102,885 px；**3 套皮肤**（default 是空壳） | 页名规范化 + **页图缩回声明尺寸** + 去污染 + 别名分组 + 选 `elite` 皮肤出图 |

---

## 九、批量处理新踩到的八个坑（2026-09）

### 9.1 页名后缀可能与实际文件不符 —— 找不到图时**报错，不要静默跳过**

`diagnose` 原来按 atlas 的页名硬拼路径（`os.path.join(dir, p['name'])`）。
Kimchul/NPC2/Nomad 的页名是 `Xxx.webp`、磁盘上却是 `Xxx.png`，于是**整个页被 `continue` 跳过**，
体检报告里只剩 `page` / `regions` 两个字段，看上去"没问题"，实则**分区性根本没算**。

* 修法：`resolve_page_image()` 依次尝试同名 → 换常见后缀（png/webp/jpg/tga/bmp）→ 大小写不敏感匹配。
* 教训：**"读不到就跳过"是体检工具最危险的写法** —— 它把"没测"伪装成"通过"。

### 9.2 页图可能被非等比重采样过 —— 用「矩形覆盖率」判定谁才是基准

Nomad 的 atlas 声明 `size: 1985,284`，给的 PNG 却是 `2048×256`。
两种可能：①页图被重采样 ②`size:` 行过期。用**可复算**的指标二选一：

```
把矩形按 (2048/1985, 256/284) 缩放后，不透明像素落在矩形内的比例 = 99.90%
不缩放                                                          = 93.26%
```

再把页图缩回 1985×284，量精灵实际像素尺寸：`blade1` 恰好 `294×50`、
`footR` 恰好 `100×55` —— **与 atlas 的 `size:` 逐字段吻合**，铁证：atlas 是基准、页图被拉伸过。
（`sqrt|det|`、`.webp→.png` 转换器都不能解释这种非等比缩放；很可能是某个工具做了 `resize(2048,256)`。）

### 9.3 「别名区域」：几何完全相同的区域是**同一张图的两个名字**

多皮肤资源会让同一张图在 atlas 里出现两次，例如 Nomad 的
`nomad_normal/torso` 与 `nomad_elite/torso` —— `xy/size/orig/offset/rotate` **逐字段一模一样**。

* 当成"互相污染"处理会把其中一个**清空**（实测占 15/43 个区域）。
* 修法：`region_groups()` 按 `(footprint, deg, ox, oy, ow, oh)` 分组，**覆盖计数按组计**、
  连通域归属也按组裁决；同组区域最终共享同一份像素。
* 顺带修掉：cha_1166 的 `l_foot_4` / `l_foot_4 _a`、cha_4205 的 `weapon` / `weapon_09`。
* ⚠️ **`diagnose` 的重叠检测也必须按组**。转换后的 cha_1166/cha_4205 图集里
  `l_foot_4 _a` 与 `l_foot_4` 占**同一个矩形**，按原始区域数就会报"1 对重叠、分区 False" ——
  假警报。按组算之后五个包的转换结果全部 `分区 True`。

### 9.4 零间距打包：矩形不重叠时，**去污染必须是恒等变换**

旧算法按「一整个连通域归一个区域」处理。Kimchul 的矩形互不重叠（重叠对 0），
但相邻精灵彼此粘连，400 个连通域里有 86 个横跨多个矩形 —— 一旦某连通域的最佳归属是别的矩形，
**本矩形的合法内容会被整块清空**：`4bd` 丢 100%、`1bd` 丢 100%、全图误删 206,149 px（占内容 40%）。

正确判据分三层：

1. 别名组（§10.3）；
2. 像素只被**一个组**覆盖 → 一定属于该组，**无条件保留**（矩形不重叠时这就覆盖了全部像素）；
3. 像素落在**多个组的重叠区** → 才靠「连通域 IoU 最大」裁决。

外加保守兜底：某区域**私有区**（矩形内只被本组覆盖的部分）占比 < 1%
**且**仲裁要拿走一半以上 → 判定"没有独立证据"，**回退原始裁切**而不是交付空白图
（Nomad 的 `bag` 被 `nomad_elite/waist2` 整个包住，private 0.0%）。
两个条件同时成立才回退，所以 private 高时天然是无操作 —— 回归验证：
cha_1114 原始 3.8 图集上，新算法与**已交付版本只差 22 像素 / 56 个区域**。

### 9.5 图片名的解析顺序是 **`path` > `name` > 附件键名**（不是只看 `path`）

```json
"armR2": { "armR2": { "name": "nomad_normal/armR2", "type": "mesh", ... } }
"blade2":{ "blade2": { "name": "blade2", "path": "blade1", "type": "mesh", ... } }
```

只看 `path` 会漏掉所有**只写 `name`** 的附件（Nomad 有 39 个），
表现为**图上只剩两只脚**，而图集里明明有这些区。
`path` 存在时优先（`blade2` 用 `blade1`）；否则用 `name`；再否则用附件键名。

另外两条同源经验：

* **要遍历所有皮肤**，只看 `skins[0]` 会把多皮肤资源的附件全判成"图集里没有"
  （Nomad 误报 40 项）—— 而**导出日志里网格名写成 `nomad_normal/armR2` 就是 Spine 自己的答案**。
* **`type` 为 `boundingbox`/`path`/`point`/`clipping` 的附件不带图片**，
  对账时要跳过，否则 `armL_path` 这类路径约束会被当成缺图。

### 9.6 预览渲染要解 IK、要挑对皮肤

* **Spine 的 setup pose 本身就解 IK。** Nomad 有 6 个 IK、Kimchul 12 个、cha_1166/cha_4205/qiaofeng 各 2 个。
  不解的话四肢会散在画布各处，看着像素材坏了。
  实现照抄官方 `IkConstraint.apply`（1 骨 / 2 骨两套），并按 `order` 排序、每次改完重算后代骨骼。
* **多皮肤资源的 `default` 可能只是个空壳。** Nomad 的 default 只有 6 项，真正的美术在
  `elite`/`normal` 各 20 项 —— 按 default 出图只有两只脚。预览要挑**部件最多的那套**，
  并且按 Spine 语义把 default 当**基底叠加**上去。
* ⚠️ 但**解了 IK 也未必"看着像个完整角色"**：Nomad 就算解了 IK，四肢仍是张开的
  （骨骼链很长而美术没填满）。这时**别急着判定渲染器有 bug** ——
  加 `--bones` 叠加骨骼，如果**每个精灵都精确落在自己的骨骼链末端**，几何就是自洽的，
  "散"是这套素材 setup pose 本来的样子。实测 Nomad 就是这种情况（IK 只让骨骼转了 1~5°）。

### 9.7 可以拿 Spine 自带的解包器当交叉验证

```bat
:: Texture atlas unpacking（官方实现）
Spine.com -u 3.8.99 -i <含atlas和页图的目录> -o <输出目录> -c <xxx.atlas>
```

对 Nomad 跑出 **43 个文件、命名与子目录结构和自己解包的完全一致**，
可直接用来验证"区域名 → 文件路径"的映射没做错。
（Spine CLI 还支持导出 **images / video**，但要一份 export settings JSON，
`class` 字段要写 Java 全类名，本轮没拿到 schema —— 需要精确对照时可再挖。）

### 9.8 「图集被打包成别的倍率」——最会伪装的一类缺陷，**步骤 1 就必须报**

它伪装得好，是因为 **Spine 3.8 按 JSON 声明的 `width/height` 画四边形**、纹理拉伸填充。
所以「声明全尺寸 + 图集只有 0.5 倍」在 3.8 里渲染出来**只是糊，几何完全正确** ——
预览图看着毫无问题。**一旦转到 4.x，导出会用图集尺寸重写附件尺寸**，
精灵各自缩水、彼此错位，直接散架。

实测（2026-09 这批，全 3.8.95）：

| 项目 | 声明/图集 不符 | 等比缩放拟合 | 转 4.3.26 后的渲染 |
|---|---|---|---|
| cha_1166 / cha_4205 | 0 / 0 | — | 正常 |
| **NPC2** | 15/19，图集 = 声明 × **0.500** | 15/15 | **头变大、四肢脱落**（包围盒 415×666 → 359×644） |
| **qiaofeng** | 7/16，图集 = 声明 × **0.500** | 7/7 | 同类 |
| **Kimchul** | 204/329，图集 = 声明 × **0.847** | 204/204 | 同类 |

判据 `size_scale_report()`：逐附件比对 `width/height` 与图集对应区域的 `orig`。
**倍率会有取整抖动**（`91/77 = 1.1818` vs `93/79 = 1.1772`，其实是同一个 0.847 被两次取整），
所以不能只看中位数就下"倍率不齐"的结论 —— 要再搜一个最优 `s`，
数 `round(声明 * s) == 图集` 能解释多少项；全部能解释才算**一次干净的等比缩放**，
那个 `s` 就是放大还原要用的倍率。

⚠️ 两个坑：

* **不能只查 `mesh` 类型** —— NPC2 / qiaofeng 的附件是 `region`，只查 mesh 会整个漏掉。
* **不能只查"JSON 旁边有单图"的情况** —— 这五个包的 zip 里都没有单图，那种校验等于没跑。

处置（照狂战士的先例）：按 `1/s` **放大图集**（页图 + 全部坐标），让图集与声明对齐，再转版本。
注意源图集已经被缩小过一次，放大只能还原几何、**不能还原清晰度**。

---

## 十、重要提醒

1. **「还原」「重打包」都不提升清晰度。** 图集信息量就那么多。要真清晰只有一条路：
   找回原始 1 倍图源，**命名与尺寸保持不变**直接替换，JSON 通常不用动。
2. **重打包是修「打包坏了」，不是修「画得糊」。** 两者别混。
3. **去污染只处理「像素归属」，不改变任何尺寸。** 交付单图尺寸 == JSON 声明尺寸。
4. **删除纪律**：`unpack` 会写出新文件，但**不要**顺手删原始件。原始三件套单独归档留档对照。
   同理，`apply` 替换顶层三件套前，先确认原始版本已在 `原始资源_*.zip` 里；
   用户没明确说要删的衍生文件（如 `_fullsize.json`）**默认保留**并主动问一句。
5. **别用一张编辑器截图就下结论。** 先确认对方用的是**哪一个入口、哪一套图**
   （loose images 还是 atlas、路径指到哪、是不是某动画的某一帧）——
   cha_1114 的排查就绕了很久，最后是靠「原画参考图 ↔ 独立渲染」对上才定了案。
6. **预览渲染的合成语义**（§3.7）值得再看一眼：`paste(mask=三角形)` 会让上层附件
   在自身透明像素处擦掉下层，症状是"某些部件没显示"。这是自己写渲染器最容易犯的错。
7. **诊断"某部件为什么没显示"的现成手段**：
   `render --bones` 把骨骼叠到预览图上；再逐件"抽掉它再渲染、看画面变不变"，
   统计有多少件 100% 不可见 —— 正确的合成下，完全被遮挡的件应该只剩真正被压住的那些。
8. **预览图通过 ≠ 转换没问题。** 预览是拿**源版本**的语义渲染的（3.8 按声明尺寸画），
   所以"打包倍率不对"这类缺陷在预览里**完全看不出来**，必须靠 §9.8 的尺寸对账去抓。
   转版本之前先跑一遍尺寸对账，比事后看图快得多。

---

## 十一、多皮肤素材专项（2026-09，`RogueSekaniFlamebringer`）

### 11.1 语义：`default` 是**基底**，命名皮肤是**叠加**在它上面

这是多皮肤最反直觉的一点。实测该素材：

| 皮肤 | 插槽 | 附件 | 内容 |
|---|---|---|---|
| `default` | 32 | 83 | **整套角色**（身体+头+手+脚+武器+火焰），但**不含**衣服那几件 |
| `elite` | 7 | 7 | 只替换 `waist` / `centerCloth` / `legL1` / `legR1` / `scarf*` |
| `normal` | 7 | 7 | 同上，另一套衣服 |

所以：

* **出预览图必须渲染「default + 该皮肤」**，而不是「只有该皮肤」——否则 elite/normal 那两张
  会缺胳膊少腿（只有几件衣服飘着）。
* **`default` 单独看是"不完整"的**（本素材就是没穿腰部那套），这是设计如此，不是坏了。
  所以**不能拿 default 的预览当验收基准**，必须逐皮肤看。
* 叠加后 `elite`/`normal` 各 90 个附件（83 + 7），比 default 多 7 个。

### 11.2 图片命名：`<皮肤>/<附件>` 与普通子目录混在一起

附件对象的 `name` / `path` 字段携带路径，实测同一个图集里三种并存：

```
armL1        -> 裸名（default 皮肤）
elite/waist  -> 皮肤子目录
fire/01      -> 普通子目录（火焰序列帧，与皮肤无关）
```

所以解包时要**建出父目录**（`os.makedirs(dirname)`），否则直接崩在
`FileNotFoundError: .../elite/waist.png`。区域名里的 `/` 在 atlas 里是合法的，
统计单图数量时也要 `os.walk` 递归，不能 `os.listdir`。

### 11.3 预览图：一套皮肤一张，命名要分流

* 单皮肤 -> `复原预览图.png`（**保持向后兼容**，前 7 个交付包都是这个名）
* 多皮肤 -> `复原预览图_<皮肤名>.png`，皮肤名里的 `/` 换成 `_`
* `apply` 要把**所有** `复原预览图*.png` 一起从版本目录搬到顶层

### 11.4 大角色的预览会直接把画布撑爆（必须限两个上限）

实测该角色 **1263×1835**：`ss=3` 的画布是 3909×5625 = **2200 万像素**，再 ×2 放大是
7600×11000，**每套皮肤一张**——渲染直接超时（10 分钟跑不完）。

两个上限，缺一不可：

* `Renderer(max_px=4_000_000)`：每个三角形都要对整张画布做仿射变换，
  开销 ≈ 画布像素 × 三角形数；超预算就自动降 `ss`（实际倍率记在 `ss_used`）。
* `render_previews(max_long=2600)`：输出长边上限。大角色按 ×2 能到近万像素、
  单张 PNG 几十 MB。

### 11.5 逐像素比对的三个坑（这一条以前一直在骗人）

比对「3.8 源渲染」与「4.x 目标渲染」时，**三个坑叠在一起**会让指标完全失真：

1. **`skeleton.x/y` 被 4.x 移除** —— 整只骨架平移。本素材平移了
   `(+595.77, +57.46)`，不补回去就是整体错位，差异被夸大成"几乎完全不同"。
   比对时必须把**源骨架的 x/y 补到目标渲染上**（`Renderer(sk_offset=...)`）。
2. **不能 `resize` 到较小尺寸再比。** 包围盒尺寸一变就是整体拉伸，
   同一只角色只因包围盒差 1.9%，指标就从 12 px 跳到 **28 万 px**。
   要**按世界坐标对齐**：图像 pixel(0,0) 对应世界 `(bb[0], bb[3])`，
   把世界原点放到画布固定位置，两张图就自然对齐。
3. **多皮肤要每套都比。** 只比一套会漏掉皮肤专属部件。

⚠️ 前几个包（cha_1114 / 狂战士 / cha_1166 / …）之所以"差异只有 9~4898 px"，
有一部分是**巧合**：它们的包围盒尺寸前后一致，按包围盒角对齐正好抵消了 `skeleton.x/y`
的平移。指标方法本身一直是错的，只是没暴露。

### 11.6 Spine 升级会**重算部分网格顶点** —— 这是官方行为，不是流程缺陷

本素材有 8 个火焰网格（`fireFan_flat*`）的 `vertices` 在转换后**长度都变了**
（877→841、558→546 等），伴随 68 处 `Reset invalid edges` 告警。
后果：`elite` 皮肤左侧那团火焰的形状与 3.8 略有差异，整体包围盒宽度 +1.93%。

判定方法：把差异画成图（红=只有源有、绿=只有目标有、蓝=两者都有）。
本素材的图**蓝色占绝对主体**（头/斗篷/裙摆/手臂/火焰都在），红绿只是 1~3px 的抗锯齿边缘
加左侧那团被重算的火焰 —— 属于可接受。

**同类素材的常识**：`Reset invalid edges` 出现在几乎所有这批包上
（cha_1114 / cha_4205 / qiaofeng / Kimchul / 本素材），是源数据网格边索引不完整的
通用问题，Spine 会自动修正。看到它不必惊慌，但**要配合差异图确认影响面**。

### 11.7 本素材同时命中了三类"老毛病"

| 问题 | 实测 | 处理 |
|---|---|---|
| 页名写 `.webp` | `RogueSekaniFlamebringer.webp` | `fix_page` 规范化 |
| **页图被非等比重采样** | 声明 1200×866，实际 1024×1024；覆盖率 79.18% → 99.88% | `fix_page` 缩回声明尺寸（H1） |
| **打包倍率 0.5×，且同图集内混倍率** | 67/94 个附件不符；**15 个是 0.5×、4 个本来就 1:1** | **逐图**按声明尺寸放大（`fit_singles_to_declared`），**不能整页放大** |

最后一条是这次新踩的：**同一个图集里可以有不同的打包倍率**。
整页 ×2 会把那 4 个本来就 1:1 的附件（`body`/`qz`/`qz2`/`wq2`）弄坏，
而且会让它们的打包矩形变成未裁白画布的两倍（荒谬）。改 JSON 也不行：改了那 4 个就错。
唯一正确的做法是**逐图**按 `declared / orig` 对齐到声明的画布尺寸。

---

## 十二、抠图判据：**优先用网格几何**（2026-09）

### 12.1 症状：精灵被切掉一半

`RogueSekaniFlamebringer` 交付后发现 `footL` 的鞋只剩左半边：

```
footL  页图原样裁切 5334 px  →  交付单图 2694 px   （删掉 2640 = 47%）
footR  页图原样裁切 4616 px  →  交付单图 1758 px   （删掉 3054）
head   内容占宽 90% / 占高 91%（底部被咬掉一口）
```

### 12.2 根因：**整页就是一个连通域**，连通域归属彻底失效

旧的去污染靠"连通域归属"裁决重叠区。但这个素材上：

```
连通域总数 = 1 个
它的面积   = 428103，bbox = (0,1,1200,865) —— 覆盖整页
```

所有精灵彼此粘连成一个巨大的 blob。算法只能把这唯一的连通域判给某一个区域，
再从别的区域的重叠区里删像素 —— **一刀切**。`footL` 与 `footR` 的矩形重叠 46×65，
重叠区里**既有 footL 自己的鞋尖、也有 footR 的鞋跟**，全删 = 鞋少一半。

⚠️ 对照：**交付后的 4.3.26 页面**上 354 个连通域**每个只对应 1 个区域**（Spine 重打包时排开了）。
所以问题只出在**原始图集**这一步 —— 别拿交付后的页面去判断原始图集好不好抠。

### 12.3 正确判据：网格几何

**网格顶点是美术自己画的，精确描述了该附件用到画布的哪一块。**

```python
# uvs 归一化在未裁白原尺寸上 -> uv * (ow, oh) 就是画布像素坐标
pts = [(uv[2*i] * ow, uv[2*i+1] * oh) for i in range(len(uv)//2)]
for 每个三角形: 在 (ow, oh) 画布上填充多边形
# 得到"这个附件真正会采样"的掩码 -> keep = 掩码（外扩 2px 防削抗锯齿边）
```

实测判别力（网格覆盖 vs 图里内容）：

| 区域 | 类型 | 网格覆盖% | 图内容% | 网格外的像素 |
|---|---|---|---|---|
| `footL` | mesh | 59.6 | 84.6 | **1614** ← 正是外来的鞋跟 |
| `shoulderR` | mesh | 64.5 | 79.1 | **1035** |
| `elite/legL1` | linkedmesh | 70.8 | 81.9 | **2547** |
| `neck` | mesh | 86.3 | 90.3 | 99 |

"网格外的那部分"就是外来像素 —— 切掉它，本体分毫不动。

### 12.4 实现要点（都是踩出来的）

* **`linkedmesh` 本身没有顶点**，要顺着 `parent` / `skin` 找到真正带网格的那个（最多追 8 层）。
* **同一张图可能被多个附件引用**（多皮肤 + `path` 指向同一图），掩码取**并集**。
* **`region` 类型附件没有网格**（整个四边形都采样）-> 该区域返回 `None`，**退回连通域判据**。
  判据优先级：网格几何 > 别名组 > 连通域仲裁（兜底）。
* **掩码要外扩 2px**，否则会把抗锯齿边削掉。
* 画布空间 -> 页内矩形空间要做**旋转的反变换**（`crop_trimmed` 用的是 `ROTATE_270`，
  反变换就是 `ROTATE_90`）。

### 12.5 验证

* 修好的：`footL` 53%→**100%**、`footR` 93%→**100%**、`head` 90%→**100%**、
  `shoulderR` 62%→**100%**、`torso` 95%→**100%**；回退区从 6 个降到 2 个。
* **回归**：拿新判据重跑**已交付的 cha_1114**，与已交付的 `images_original/` 对比 ——
  **只差 23 个像素 / 3 个区域**（旧判据当时差 22 像素）。
  **新判据在旧素材上不改变结果、在新素材上修好问题**，这是它可信的依据。

### 12.6 诊断手段：先把页面结构画出来

抠之前先出一张「**透明图层描边 + 序号**」的图：alpha 的每个连通域描边、质心标序号，
再叠一版带图集矩形和区域名的。这次就是靠它发现"整页只有一个连通域"的：

```python
lab, n = ndimage.label(alpha, structure=np.ones((3,3)))   # 连通域
edge = alpha & ~binary_erosion(alpha)                     # 描边
```

顺带一提：`region` 类型附件的 `bounds` 是**未裁白**尺寸（`orig == size`、`offset 0,0`），
所以它的矩形**本来就比精灵大**，矩形里带空白是正常的，不等于抠错了。


---

## 十三、四阶段流水线定稿 + 本轮抓出的六个真 bug（2026-09 二轮）

这一轮把流程固化成**四个阶段、产物严格分目录**，并把"验收"从"人眼看预览"
升级成"脚本先判定、人只做最后裁决"。过程中抓出**六个真 bug**，其中两个直接
影响此前 8 个已交付包的预览可信度。

### 13.1 四阶段与目录约定（**硬性**）

```
<proj>/
├── prepare/    阶段1：源版本三件套（页名/页尺寸已修）+ images_original/ + 预览
├── convert/    阶段2：目标版本三件套 + 预览
├── （顶层）     阶段3 apply 之后的交付件：三件套 + images_original/ + 预览 + 交付说明.md
├── 原始资源_<名>.zip              回退用，永不动
├── 失败_需人工复核.md             仅判定不一致时出现
└── 对比图_<皮肤>.png              仅判定不一致时出现
```

**为什么必须分目录**：`convert` 只写 `convert/`，**顶层一个字节都不动** ——
所以可以反复跑 `convert`、反复看它的预览，零风险；`apply` 是**唯一会动顶层**的阶段。

一条命令跑完前三阶段：

```powershell
python tools\repair_spine\pipelines.py build "<源zip>" -o "<工程目录>"
```

### 13.2 `apply` 的判定：**先比渲染，一致才落盘**

```
apply
  ├─ 逐皮肤渲染比对：同一套 images_original/，
  │   分别用 prepare/ 的 JSON 与 convert/ 的 JSON 渲染，比 alpha 剪影
  ├─ 一致   -> 顶层替换 + 单图搬上来 + 预览上提 + 删掉 convert/ 与 prepare/ + 交付说明
  └─ 不一致 -> 顶层不动 + prepare/ 与 convert/ 两份都留 + 对比图_*.png
               + 失败_需人工复核.md + 退出码 2
```

* **不比预览 PNG 文件**：4.x 移除了 `skeleton.x/y`，两边预览的画布尺寸和原点都不同，
  直接逐像素相减全是假差异。正确做法是**按世界坐标对齐后再比**（渲染时就对齐）。
* **必须逐皮肤比**：多皮肤资源只比一套会漏掉皮肤专属部件。
* **必须补偿源骨架的 `skeleton.x/y`**：不补就整体平移（实测 595.77px）。
* **判定要看"带容差的硬差异"**，不能看原始 XOR（见 13.8）。

### 13.3 bug①：`linkedmesh` 没分流，被当成矩形画

```python
if a.get('type') == 'mesh':
    mesh_geometry(...)
else:
    region_quad(...)          # ✗ linkedmesh 掉进这里
```

`linkedmesh` 的 `type` 不是 `'mesh'`，它**没有 `x`/`y`/`rotation`**（那些在父网格的
顶点里），于是被画成**以骨骼原点为中心的横平竖直大矩形**。表现：**大腿/裙子错位、
腿和身体断开**。修法是先解引用再分流：

```python
geo = deref_linkedmesh(d, a)
if geo and 'mesh' in str(geo.get('type')) and geo.get('uvs'):
    mesh_geometry(geo, ...)
else:
    region_quad(a, ...)
```

### 13.4 bug②：Spine 4.x 把 `linkedmesh` 的父指针键名改了

```
3.8.95   {"type":"linkedmesh", "parent":"legL1", "skin":"normal"}
4.3.26   {"type":"linkedmesh", "source":"legL1", "skin":"normal"}
```

**`parent` -> `source`**。两个都要认：

```python
pn = a.get('parent') or a.get('source')      # 4.x 用 source
```

不认的后果同 13.3。**这个 bug 让 `convert` 的渲染对比给出假数字**（目标侧渲染是坏的），
实测 `elite` 的 IoU 从假的 **0.590** 变成真的 **0.812**。

### 13.5 bug③：旋转区域的网格掩码整块错位

```python
x, y, w, h = footprint(r)        # ✗ footprint 对 deg 90/270 做过宽高互换
top = r['oh'] - r['oy'] - h      # 用互换过的 h 算偏移 -> 错
sub = m[top:top+h, left:left+w]  # armR2 切出 (77,77)，画布是 (122,77) -> 补零 -> 掩码错
```

**切画布必须用区域自己的 `r['w']`/`r['h']`**，`footprint()` 的返回值是"页内矩形"的尺寸。
实测影响：`armR1_sleeve` 4943 -> **7530**、`legR2` 6556 -> **10112**（之前一直在多切）。

### 13.6 bug④：CLI 默认值与函数默认值不一致

`prepare()` 函数默认 `overlap='mesh'`，而 CLI 的 `--overlap` argparse 默认还是 `'keep'` ——
命令行不传参跑出来是"不裁剪"，和交付口径相反。表现是"去污染"那行根本不打印（清理 0 像素）。

**教训**：默认值只写一处，或至少写完后跑一次"不带任何可选参数"的冒烟。

### 13.7 渲染提速 30~40 倍：**每个三角形只在自身包围盒里做仿射变换**

原来是每个三角形都对**整张画布** `Image.transform` 一次；一个角色几百个三角形
就是几百次全图运算。改成先算三角形的画布包围盒，把矩阵平移过去，只变换那一小块：

```python
bx0, by0, bx1, by1 = 三角形包围盒（各外扩 1~2px 防滤波溢出）
tx = Mi[0,0]*bx0 + Mi[0,1]*by0 + Mi[0,2]     # 矩阵平移到局部坐标
ty = Mi[1,0]*bx0 + Mi[1,1]*by0 + Mi[1,2]
tex = src.transform((bx1-bx0, by1-by0), Image.AFFINE,
                    (Mi[0,0], Mi[0,1], tx, Mi[1,0], Mi[1,1], ty), resample=BILINEAR)
```

实测：**每套皮肤 32~41 秒 -> 1 秒**；整条流水线 **349 秒 -> 27 秒**。

**等价性必须验证**（不能只看"跑得快了"）：写隔离测试 —— 同一附件、同一批三角形，
新旧两条路径各画一遍比像素。实测 6 个附件**全部 0 差异**。文件字节数会变
（浮点舍入让抗锯齿边缘略有不同）**不能当等价性证据**。

### 13.8 判据要有**像素容差**，但也要知道它救不了什么

剪影整体平移 1px 会让 alpha XOR 抬到二三十个百分点（一条 2px 宽的轮廓挪 1px，
两侧各翻出一片像素），肉眼却完全一样。所以判定用**带容差的硬差异**：

```python
ma_d = binary_dilation(ma, iterations=tol_px)    # 默认 tol_px=1
mb_d = binary_dilation(mb, iterations=tol_px)
hard = (ma & ~mb_d) | (mb & ~ma_d)               # 真正"多出/丢失"的形状
```

**但要清醒**：本轮实测加 1px 容差只把 27.62% 降到 25.48% —— **几乎没用**，
说明差异不是亚像素抖动，是**真的几何变化**。于是进一步**定位到具体部件**：
逐附件比对新旧网格顶点的最大位移 —— `legL1/L2` **66~68px**、`legR1/R2` **40~41px**、
`fireFan_flat*` **41.7px**。这些正是 Spine 日志里 `Reset invalid edges`（68 处）
重建过的网格。

**结论**：这是**源数据的固有缺陷**，不是流程缺陷；判定为"不一致"是**真阳性**。
阈值定在哪里是**人的决策**，脚本只负责把数字和对比图摆出来。

### 13.9 最贵的一课：**校验工具自己也要能被验证**

第一次跑 `convert` 时它报 `elite IoU 0.590`，我差点据此判定"转换严重失真"。
实际是**渲染器有 bug**（13.3 + 13.4），目标侧的渲染本身就是坏的 —— 数字是假的。

**纪律**：
* 校验链路里的每个环节（渲染器、对齐、指标）都要有**自检**：
  同一输入渲染两次应逐像素相同；对齐后同一张图 IoU 应为 1.000。
* 指标突然变差时，**先怀疑工具，再怀疑数据** —— 尤其是刚改过工具之后。
* 关键结论要**两条独立路径互证**：数字（IoU）+ 图像（差异染色图）。

### 13.10 失败时要交**对比图**，不是只交一个数字

判定不一致时自动生成逐皮肤的三格对比图：

```
左 prepare（源） | 中 convert（目标） | 右 差异染色
```

| 颜色 | 含义 |
|---|---|
| **红** | 只有源有 -> 目标丢了 |
| **绿** | 只有目标有 -> 多了 |
| **蓝** | 两边都有但 RGB 差 > 24 |
| **灰白** | 两边一致 |

对比图和判定**共用同一套对齐逻辑**（`_align_render_pair` + 骨架 `x/y` 补偿），
保证"图里看到的差异"和"判定用的数字"是同源的。失败说明 `失败_需人工复核.md`
里列出这些图并注明配色 —— 拿到就能看，不用再找脚本。

### 13.11 交付归档：**分类与命名是 agent 的职责**（不是可选项）

这是流水线的第 4 步，也是**唯一一步脚本干不了、必须 agent 干**的事：

```
python tools\repair_spine\pipelines.py file "<工程目录>" --category "人形/女" --as "火把女战士"
```

归档到 **`D:\artstudio\assets\2d\<分类>\<目录名>\`**（硬性，根目录由
`ASSETS_2D` 常量固定，不要往别处放）。

**分工（硬性）**

| 谁 | 干什么 |
|---|---|
| 脚本 `file` | 确认顶层确实是交付件 -> 原样搬运 -> 报告覆盖了什么。**不猜分类、不起名** |
| **agent（大模型）** | 看 `复原预览图_*.png` 判断**这是什么、长什么样**，定出 `--category` 与 `--as` |

⚠️ **这一步属于 agent，不是"待人工处理"** —— 用户不会替你做，脚本也做不了。
判定依据只看**外观**：武器、服饰、发色、体型、配饰
（**不要用文件名暗示** —— `cha_1114`、`NPC2` 这类代号本身不含信息）。

**命名规则**

* `--as` 用**纯中文人物特征**：`兽人狂战士`、`斗笠剑客`、`火把女战士`、
  `光头打手`、`红发法师`、`白袍叶灵`……**不要混英文，也不要带源文件名**。
* **只改分类目录名，包内文件名一律不动**。可追溯性由包内的
  `原始资源_<原名>.zip` 保证（原文件名就在归档里）。
* **内部文件名不能改**：atlas 基名决定图集名，JSON 里的引用也全指着它，改了直接断。
* 不传 `--as` 时脚本按顶层 JSON 名兜底（会出现英文/源文件名）—— **不要依赖这个兜底**。
  尤其当项目目录名与 atlas 基名不一致时（如 `狂战士/` 里是
  `Unit_Spine_NowsiWarriorSpe1.*`），兜底会建出英文目录。

**批量处理时**：先把所有包的预览图拼成一张总览图（网格排布），
**一次看完再统一起名** —— 比逐个打开省事，也更容易发现重名。

### 13.12 本轮流程跑批的结果

8 个包（`assets/2d/人形/{男,女}/`）全部用新流程重跑：

| 项目 | 单图 | 分类目录（重命名后） |
|---|---|---|
| cha_1114 | 56 | 男/斗笠剑客 |
| Kimchul | 182 | 男/光头打手 |
| NPC2 | 19 | 男/络腮胡剑士 |
| qiaofeng | 16 | 男/黄衣游侠 |
| Unit_Spine_NowsiWarriorSpe1 | 23 | 男/兽人狂战士 |
| cha_1166 | 69 | 女/红发法师 |
| cha_4205 | 63 | 女/白袍叶灵 |
| RogueSekaniFlamebringer | 55 | 女/火把女战士 |

8 个全部通过渲染一致性判定，总计约 **2.5 分钟**（旧流程这个量级要半小时以上）。

⚠️ **这 8 个包此前的预览图全部不可信** —— 它们是旧渲染器出的，受 13.3 / 13.4 两个 bug
影响（`linkedmesh` 被当矩形画）。**据此做过的"渲染 IoU 0.73~0.79"之类的判断都要作废**，
本轮的才是真的。


### 13.13 bug⑤：**网格附件的 `width`/`height` 可能是占位值**，不能拿去缩放图

`fit_singles_to_declared` 的逻辑是"按 JSON 声明的 `width/height` 把单图对齐到声明尺寸"，
用来修"图集被打包成别的倍率"。但它**默认所有附件都适用** —— 而**网格附件的声明尺寸
可能是占位值**：

```
实测 S041_skin6（3.8.95，65 个附件）
  body        mesh  声明 32x32   实际渲染 168x270
  hair-back   mesh  声明 32x32   实际渲染 343x434
  skirt-rear  mesh  声明 32x32   实际渲染 282x252
  skirt-down  mesh  声明 32x32   实际渲染 101x235
```

网格的真实形状由 `vertices` 决定、图尺寸由图集 `orig` 决定，**声明值毫无参考价值**。
于是这 5 张网格图被缩成 32x32，再被网格拉伸到几百像素 —— 画面里出现
**大块矩形残影**（红色长发变成一块红矩形、白毛皮裙变成一块灰矩形）。
诊断时的典型特征是「**N 个附件不符，而 N 恰好等于网格数**」。

**修法**：`declared` 映射里**跳过所有 `mesh` 类型附件**（含 `linkedmesh`/`weightedmesh`）：

```python
if 'mesh' in str(a.get('type')):
    continue        # 网格的 width/height 可能是占位值，跳过
```

实测修后"单图按声明尺寸放大"从 **5 张 -> 0 张**，渲染恢复正常。

**推广的教训**：凡是要拿 JSON 的 `width`/`height` 当"真值"的地方，都要先问一句
**"这个附件的类型，`width/height` 可靠吗？"** —— 网格不可靠，区域附件可靠。

**脚本已内置检测**（`size_scale_report` 返回 `placeholder_suspect` / `mesh_total` /
`mesh_mismatch`，`prepare` 命中就打印告警）：

```python
# 诊断特征：不符的那批恰好全是网格附件，且数量等于网格总数
#   实测 S041_skin6：5 个网格全写 32x32 -> 5/67 不符 -> 命中
# 另加一条：单个网格声明值与图集 orig 差 2 倍以上也算嫌疑
wild = [b for b in bad if b[0] in mesh_bad and (b[3] > 2.0 or b[3] < 0.5)]
out['placeholder_suspect'] = bool(mesh_bad) and (
    (len(bad) == mesh_seen and mesh_seen >= 2) or len(wild) >= 1)
```

命中的输出：

```
修复  ⚠ 网格附件的声明尺寸疑为**占位值**（5 个网格中 5 个与图集不符，如 skin6/body）
      —— 已跳过它们不做尺寸对齐；若不跳过会把网格图缩成声明值，
      再被网格拉伸成大块矩形残影
```

**误报控制**：`RogueSekaniFlamebringer`（网格声明尺寸是真实值，如 `armL1 99x106`）
实测**不命中** —— 因为它的不符项里混着区域附件，数量也不等于网格总数。


### 13.14 「只能靠网格」的场景怎么防：**提前做网格体检**

13.13 的结论是"网格不可靠、区域附件可靠"。但**图集两两重叠时，只有网格能判
"这块像素归谁"** —— 连通域在这种素材上几乎必然失效（实测某素材整页 1200x865
就是一个连通域）。所以网格一旦不准，**那个场景没有别的判据可退**。

**必须在动手裁之前把"无效网格"查出来**。判据 —— **网格能不能解释它自己区域里的内容**：

```
content_px  = 本区域内容的不透明像素数（画布空间）
mesh_px     = 网格掩码像素数
covered_px  = |网格 ∩ 内容|
cover_ratio = covered_px / content_px     # 网格解释了本区域多少本体
fill_ratio  = covered_px / mesh_px        # 网格自己有多少落在内容上
```

| 指标 | 含义 | 判定 |
|---|---|---|
| `cover_ratio ≈ 1` | 网格精确覆盖本体 | **可信** |
| `cover_ratio << 1` | 网格**小于**本体，漏掉的是本体 | **不可信** —— 按它切会**削掉美术** |
| `fill_ratio << 1` | 网格大部分落在空白上 | **不可信** —— 网格位置/形状不对 |

实现：`mesh_health(atlas, json, min_cover=0.75)`，返回逐网格的四个数 + `ok`/`why`。
`prepare` 会自动调用并打印：

```
修复  ⚠ 有 16/38 个网格**不可信**（按网格裁会削本体）：
      hair(网格偏小（只解释 48% 的本体）)、fireFan_flat(59%)、hornL(59%)、…
```

**实测判别力**

| 素材 | 网格体检 | 说明 |
|---|---|---|
| `RogueSekaniFlamebringer` | **16/38 不可信**（最低 48%） | 正是那个"按网格切会削本体"的素材 |
| `S041_skin6` | **5/5 可信** | 5 个网格里 3 个切 0 px、2 个只切边角 |

**注意：这是"只报告、不改行为"的预检** —— 不可信的区域按网格裁会削本体，
但**是否改用 `overlap='keep'` 由人/agent 决定**（不能悄悄换判据，见第十一章的教训）。
`prepare` 的报告里能看到，`overlap='keep'` 是现成的逃生开关。

**局限性（要说清）**：`cover_ratio` 低有两种可能 —— **网格偏小**，或者**本区域内容
被邻居污染了**。两者从数字上分不开（因为"内容"本身包含邻居）。
所以体检报的是"**这里需要人看一眼**"，不是"网格一定坏了"。这就是它的价值：
**把需要人判断的地方提前圈出来，而不是等交付完才发现。**
