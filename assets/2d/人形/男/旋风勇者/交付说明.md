# 交付说明 —— let-me-solo-all

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 16 / 14 / ['default', '1', '2', '3'] |
| 附件总数 | 47（图集区域 34） |
| 动画 | 8 个：attack, damage, death, idle, ready, run, spawn, ultimate-attack |

## 交付物

* `images_original/`  (34 张单图)
* `let-me-solo-all.atlas`  (1230 字节)
* `let-me-solo-all.json`  (22445 字节)
* `let-me-solo-all.png`  (689669 字节)
* `原始资源_let-me-solo-all.zip`  (708955 字节)
* `复原预览图_1.png`  (561781 字节)
* `复原预览图_2.png`  (518529 字节)
* `复原预览图_3.png`  (551732 字节)
* `复原预览图_default.png`  (327826 字节)

## 处理动作

* ⚠ 有 8/27 个网格**不可信**（按网格裁会削本体）：2/head(网格多半落在空白上（49% 命中内容）)、2/sword_l(网格多半落在空白上（22% 命中内容）)、2/sword_r(网格多半落在空白上（31% 命中内容）)、3/arm_r(网格多半落在空白上（48% 命中内容）)

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 34 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 34/34 一致（RGB 最大差 0，alpha 最大差 0） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_let-me-solo-all.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_let-me-solo-all.zip -o <空目录>
```

## 备注

* Spine 重置无效边 32 处（源数据的网格边索引不完整，Spine 自动修正；该素材家族的常见告警）
