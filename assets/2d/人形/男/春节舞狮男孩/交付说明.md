# 交付说明 —— nanzhu_manianchunjie

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 127 / 83 / ['default'] |
| 附件总数 | 88（图集区域 83） |
| 动画 | 16 个：2344, Atk, Death, Reload, Reload_Run, Reload_Run_Weak, Reload_Weak, Run_Fight |

## 交付物

* `images_original/`  (83 张单图)
* `nanzhu_manianchunjie.atlas`  (3233 字节)
* `nanzhu_manianchunjie.json`  (1433718 字节)
* `nanzhu_manianchunjie.png`  (829167 字节)
* `原始资源_nanzhu_manianchunjie.zip`  (966499 字节)
* `复原预览图.png`  (1318210 字节)

## 处理动作

* ⚠ 页 `size:` 行过期（声明 [984, 984] 实际 [985, 985] 只差几像素、覆盖率 99.79 无法区分）—— 已按**实际图**改 `size:` 行、**不重采样**（改声明无损，缩图会让全图重采样）
* ⚠ 有 10/55 个网格**不可信**（按网格裁会削本体）：zuiba2(网格偏小（只解释 19% 的本体）；网格多半落在空白上（41% 命中内容）)、bp_bianpaoyou5(网格偏小（只解释 31% 的本体）)、bp_bianpaozuo4(网格偏小（只解释 34% 的本体）)、zuoshou(网格偏小（只解释 35% 的本体）；网格多半落在空白上（38% 命中内容）)
* 单图按声明尺寸放大 ×1.0137：19 张（源图集 = 声明尺寸 × 0.986）
* 网格附件里 2 个的声明尺寸与包内倍率自洽，一并还原
* **源图集尺寸自检**：源图集 = 声明尺寸 × 0.986（20/88 个附件不符，一次等比缩放可解释 18/18）

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 83 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 79/83 一致（RGB 最大差 0，alpha 最大差 255） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_nanzhu_manianchunjie.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_nanzhu_manianchunjie.zip -o <空目录>
```

## 备注

* Spine 重置无效边 114 处（源数据的网格边索引不完整，Spine 自动修正；该素材家族的常见告警）
