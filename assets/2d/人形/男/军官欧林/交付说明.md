# 交付说明 —— OfficerErrin

## 版本

| 项 | 值 |
|---|---|
| 源版本 | Spine **3.8.95** |
| 交付版本 | Spine **4.3.26** |
| 骨骼 / 插槽 / 皮肤 | 31 / 23 / ['default'] |
| 附件总数 | 32（图集区域 27） |
| 动画 | 11 个：idle_aggressive, idle_aggressive_mannerism, pose_attack, pose_cast_spell, pose_death, pose_defend, pose_hurt, pose_move_back |

## 交付物

* `OfficerErrin.atlas`  (866 字节)
* `OfficerErrin.json`  (111052 字节)
* `OfficerErrin.png`  (325803 字节)
* `images_original/`  (27 张单图)
* `原始资源_OfficerErrin.zip`  (221812 字节)
* `复原预览图.png`  (1094423 字节)

## 处理动作

* 页名 OfficerErrin.webp -> OfficerErrin.png
* 页图缩回声明尺寸 [1024, 256] -> [1500, 234]
* ⚠ 有 5/22 个网格**不可信**（按网格裁会削本体）：neck(网格偏小（只解释 63% 的本体）)、armR1(网格偏小（只解释 69% 的本体）)、legR1(网格偏小（只解释 70% 的本体）)、spear(网格偏小（只解释 71% 的本体）)
* ⚠ 5 个区域无私有区，已回退原始裁切：['eye', 'eye_closed', 'mouth', 'mouth_clenched', 'mouth_open']
* 单图按声明尺寸放大 ×2.0000：5 张（源图集 = 声明尺寸 × 0.500）
* **源图集尺寸自检**：源图集 = 声明尺寸 × 0.500（5/30 个附件不符，一次等比缩放可解释 5/5）

## 校验（可复算）

| 指标 | 结果 |
|---|---|
| 图集区域数 | 27 |
| 矩形构成页图精确分区 | **True**（多重覆盖 0，未覆盖 0，重叠对 0） |
| 三方对账（JSON↔atlas↔磁盘） | 缺 无 / 多 无 |
| 反解回环（atlas → 单图） | 27/27 一致（RGB 最大差 0，alpha 最大差 0） |
| 附件尺寸 vs 图集 `orig` | 不符 0 处 |

## 回退

原始三件套完整保存在 `原始资源_OfficerErrin.zip` 里，随时可回退：

```bash
python tools/repair_spine/pipelines.py build 原始资源_OfficerErrin.zip -o <空目录>
```

## 备注

* Spine 重置无效边 50 处（源数据的网格边索引不完整，Spine 自动修正；该素材家族的常见告警）
